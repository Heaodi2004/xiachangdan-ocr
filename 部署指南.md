# 部署指南：Hugging Face Spaces

本文档详细说明如何将下场单智能生成系统部署到 **Hugging Face Spaces**，成为公开可访问的网站。

---

## 目录

- [为什么选择 Hugging Face Spaces](#为什么选择-hugging-face-spaces)
- [部署方式对比](#部署方式对比)
- [第一部分：上传到 GitHub](#第一部分上传到-github)
- [第二部分：部署到 Hugging Face Spaces](#第二部分部署到-hugging-face-spaces)
- [第三部分：本地 Docker 测试（可选）](#第三部分本地-docker-测试可选)
- [常见问题](#常见问题)
- [部署检查清单](#部署检查清单)

---

## 为什么选择 Hugging Face Spaces

### ✨ 核心优势

| 特性 | Hugging Face Spaces | PythonAnywhere (免费版) |
|------|---------------------|------------------------|
| **内存** | **16 GB** | 512 MB |
| CPU 核心 | 2 核 | 1 核（共享） |
| 休眠时间 | 48 小时无活动 | 24 小时无活动 |
| 部署方式 | Docker（灵活） | 传统 Web 应用 |
| 构建日志 | 实时查看 | 有限 |
| 社区展示 | 可被其他用户发现 | 不支持 |

### 🎯 为什么适合这个项目

本项目使用 **EasyOCR + PyTorch**，内存占用约 1-2GB：
- PythonAnywhere 免费版（512MB）→ ❌ 内存不足
- Hugging Face Spaces（16GB）→ ✅ 绰绰有余

---

## 部署方式对比

### 方式一：GitHub 自动部署（推荐）

- ✅ 代码更新自动重新构建
- ✅ 版本管理方便
- ✅ 可以在本地开发，推送到 GitHub 自动部署
- ⚠️ 需要 GitHub 账号

### 方式二：Hugging Face Git 仓库

- ✅ 不需要 GitHub 账号
- ✅ 直接用 Hugging Face 提供的 Git 仓库
- ⚠️ 手动上传代码

> 本文档以 **方式一（GitHub 自动部署）** 为例，这是最常用也最方便的方式。

---

## 第一部分：上传到 GitHub

### 前置准备

1. **注册 GitHub 账号**：如果还没有，前往 [github.com](https://github.com) 注册
2. **安装 Git**：
   - Windows：下载 [Git for Windows](https://git-scm.com/download/win) 并安装
   - 安装时保持默认选项即可
   - 验证：打开命令行，输入 `git --version`

### 步骤 1：在 GitHub 创建新仓库

1. 登录 GitHub，点击右上角 **+** → **New repository**
2. 填写仓库信息：
   - **Repository name**：仓库名称（例如：`xiachangdan-ocr`）
   - **Description**（可选）：基于OCR的下场单智能生成系统
   - **Public**：公开仓库（推荐，Hugging Face 可以直接访问）
   - **Initialize this repository with**：**全部不要勾选**
3. 点击 **Create repository**
4. 创建成功后，复制仓库地址（HTTPS格式，例如：`https://github.com/你的用户名/仓库名.git`）

### 步骤 2：本地初始化并上传

打开命令行（PowerShell 或 CMD），进入项目目录：

```bash
cd C:\Users\he\Desktop\下场单
```

执行以下命令：

```bash
# 1. 初始化 Git 仓库（如果还没有）
git init

# 2. 设置用户名和邮箱（第一次使用 Git 需要）
git config user.name "你的GitHub用户名"
git config user.email "你的GitHub邮箱"

# 3. 添加所有文件
git add .

# 4. 提交代码
git commit -m "初始提交：下场单智能生成系统"

# 5. 关联远程仓库（替换为你自己的仓库地址）
git remote add origin https://github.com/你的用户名/仓库名.git

# 6. 重命名主分支为 main
git branch -M main

# 7. 推送到 GitHub
git push -u origin main
```

### 步骤 3：验证上传成功

刷新你的 GitHub 仓库页面，确认以下文件都已上传：
- `app.py`
- `Dockerfile`
- `requirements.txt`
- `README.md`
- `templates/` 目录
- `.gitignore`
- `.dockerignore`

### 后续更新代码

修改代码后，推送到 GitHub：

```bash
git add .
git commit -m "修改说明"
git push
```

推送成功后，Hugging Face Spaces 会**自动重新构建**。

---

## 第二部分：部署到 Hugging Face Spaces

### 步骤 1：注册 Hugging Face 账号

1. 访问 [huggingface.co](https://huggingface.co)
2. 点击右上角 **Sign Up**
3. 填写邮箱、用户名、密码
4. 验证邮箱
5. 登录账号

### 步骤 2：创建新 Space

1. 点击头像 → **New Space**
   或者直接访问：[huggingface.co/spaces](https://huggingface.co/spaces) → **Create new Space**

2. 填写 Space 信息：

   | 字段 | 填写内容 | 说明 |
   |------|---------|------|
   | **Space name** | 你的 Space 名称 | 例如：`xiachangdan-ocr` |
   | **License** | MIT License | 开源协议 |
   | **Space SDK** | **Docker** | 选择 Docker，不要选 Gradio/Streamlit |
   | **Docker template** | Blank / Empty | 空白模板 |
   | **Space hardware** | **CPU basic** | 免费 2 核 16GB |
   | **Public** | Public 或 Private | 公开或私有 |

3. 点击 **Create Space**

### 步骤 3：连接 GitHub 仓库（自动部署）

有两种方式可以部署：

#### 方式 A：GitHub 自动同步（推荐）

1. 在 Space 页面，点击 **Files** 选项卡
2. 点击右上角三个点 → **Add file** → 可以先跳过
3. 在 Space 的 **Settings** 选项卡中
4. 找到 **GitHub integration** 部分
5. 点击 **Connect GitHub repository**
6. 选择你刚才创建的 GitHub 仓库
7. 配置：
   - Source：GitHub
   - 仓库：`你的用户名/仓库名`
   - 分支：`main`
   - 目录：根目录 `/`
8. 点击 **Connect**
9. Hugging Face 会自动从 GitHub 拉取代码并构建

#### 方式 B：手动上传文件

如果不想用 GitHub，也可以直接在 Hugging Face 上传文件：

1. 在 Space 页面，点击 **Files** 选项卡
2. 点击 **Add file** → **Upload files**
3. 把以下文件拖拽上传：
   - `app.py`
   - `Dockerfile`
   - `requirements.txt`
   - `templates/index.html`
   - 其他文档（可选）
4. 点击 **Commit changes**

> **注意**：`templates/` 是目录，需要先在 Files 页面创建目录，再上传里面的文件。

### 步骤 4：等待构建

1. 切换到 **App** 选项卡
2. 可以看到构建日志（Building）
3. 第一次构建需要较长时间（5-20 分钟），因为：
   - 下载 Python 基础镜像
   - 安装 PyTorch 和 EasyOCR
   - 下载 OCR 模型
4. 看到 **Running** 状态表示构建成功

### 步骤 5：访问网站

构建成功后，你的网站地址是：
```
https://你的用户名-你的space名.hf.space
```

例如：`https://zhangsan-xiachangdan-ocr.hf.space`

点击 Space 页面的 **App** 选项卡也可以直接访问。

---

## 第三部分：本地 Docker 测试（可选）

在部署到 Hugging Face 之前，可以先在本地测试 Docker 镜像是否正常工作。

### 前置准备

1. 安装 Docker Desktop
   - Windows：下载 [Docker Desktop](https://www.docker.com/products/docker-desktop/)
   - 安装后重启电脑
   - 验证：打开命令行，输入 `docker --version`

### 本地构建和测试

```bash
# 进入项目目录
cd C:\Users\he\Desktop\下场单

# 构建 Docker 镜像（需要 5-15 分钟）
docker build -t xiachangdan .

# 运行容器
docker run -p 7860:7860 xiachangdan

# 访问 http://127.0.0.1:7860
```

### 常用 Docker 命令

```bash
# 查看运行中的容器
docker ps

# 停止容器
docker stop <容器ID>

# 查看镜像列表
docker images

# 删除镜像
docker rmi xiachangdan
```

---

## 常见问题

### 部署相关

#### Q: 构建失败了怎么办？

**A**: 查看构建日志中的错误信息，常见原因：
1. **依赖安装失败** → 检查 `requirements.txt` 中的版本是否兼容
2. **Dockerfile 语法错误** → 检查 Dockerfile 格式
3. **网络问题** → 重新构建一次试试

#### Q: 构建成功但 App 打不开？

**A**: 可能的原因：
1. 端口不对 → 确保 Dockerfile 中暴露的是 `7860` 端口
2. 启动命令错误 → 检查 Dockerfile 的 CMD 指令
3. OCR 模型下载失败 → 查看日志，可能是网络问题

#### Q: 第一次打开很慢？

**A**: 正常现象：
- Space 休眠后第一次访问需要唤醒（约 30-60 秒）
- OCR 模型加载需要时间（约 10-30 秒）
- 之后访问就快了

#### Q: 48 小时后 Space 休眠了怎么办？

**A**: 免费版的限制，解决方法：
1. 手动访问一次即可唤醒
2. 使用 UptimeRobot 等监控服务定时访问
3. 升级到付费版（不休眠）

### OCR 相关

#### Q: 识别速度很慢？

**A**: 
- 第一次识别需要加载模型，会慢一些
- CPU 版本的 EasyOCR 识别一张图片约 2-5 秒
- 可以考虑升级 GPU 硬件（付费）

#### Q: 中文识别效果不好？

**A**: 
- 确保图片清晰、光线充足
- 拍摄角度正对铭牌
- EasyOCR 的中文识别已经很不错了，但复杂场景可能需要专门训练

### 功能相关

#### Q: 上传的图片会保存多久？

**A**: 
- 上传的图片保存在容器内的 `uploads/` 目录
- 如果 Space 重启或重新构建，文件会丢失
- 重要文件请及时下载保存

#### Q: 可以自定义域名吗？

**A**: 
- 免费版使用 `hf.space` 子域名
- 付费版支持自定义域名

#### Q: 可以设置访问密码吗？

**A**: 
- 可以将 Space 设为 **Private**（只有你能访问）
- 或者在代码中添加登录认证（需要自己实现）

---

## 部署检查清单

部署前确认：

- [ ] GitHub 仓库已创建，代码已推送
- [ ] `Dockerfile` 文件存在
- [ ] `requirements.txt` 包含所有依赖
- [ ] `.dockerignore` 文件存在
- [ ] `app.py` 中端口配置正确（7860）
- [ ] `templates/index.html` 文件存在
- [ ] 本地测试过可以正常运行

部署后验证：

- [ ] Space 构建成功（Running 状态）
- [ ] 可以正常访问首页
- [ ] 页面样式正常
- [ ] 图片上传功能正常
- [ ] OCR 识别功能正常
- [ ] Excel 导出功能正常
- [ ] Excel 文件可以正常下载

---

## 后续优化建议

1. **添加访问统计**：集成 Google Analytics 或其他统计工具
2. **添加用户反馈**：嵌入评论或反馈表单
3. **添加示例图片**：在页面上放几张示例铭牌图片供测试
4. **优化首页加载**：可以先显示加载动画，OCR 初始化完成后再允许上传
5. **添加多语言**：支持中英文切换

---

## 相关资源

- [Hugging Face Spaces 文档](https://huggingface.co/docs/hub/spaces)
- [Docker 入门教程](https://docs.docker.com/get-started/)
- [Flask 官方文档](https://flask.palletsprojects.com/)
- [EasyOCR GitHub](https://github.com/JaidedAI/EasyOCR)

---

**遇到问题？**
1. 先查看 Hugging Face 的构建日志和运行日志
2. 检查本指南的"常见问题"部分
3. 参考 [使用教程.md](./使用教程.md) 了解更多功能说明
