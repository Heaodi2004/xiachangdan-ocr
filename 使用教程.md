# 下场单智能生成系统 使用教程

## 目录

- [系统简介](#系统简介)
- [功能特性](#功能特性)
- [技术栈](#技术栈)
- [环境要求](#环境要求)
- [安装部署](#安装部署)
- [使用指南](#使用指南)
- [API 接口文档](#api-接口文档)
- [常见问题](#常见问题)
- [目录结构](#目录结构)

---

## 系统简介

下场单智能生成系统是一款基于 OCR 光学字符识别技术的设备下场单自动化生成工具。用户只需上传设备铭牌照片，系统即可自动识别设备信息并生成规范的 Excel 下场单，大幅提升工作效率。

## 功能特性

### 核心功能

1. **图片自动识别**：支持拖拽或点击上传设备铭牌图片，自动识别设备信息
2. **多类别管理**：支持外检设备、带回设备、退检设备三种分类
3. **智能信息提取**：自动提取设备名称、规格型号、出厂编号、生产厂家等关键字段
4. **Excel 一键导出**：生成规范格式的下场单 Excel 表格，支持自定义公司信息
5. **OCR 原始内容展示**：展示 OCR 识别的原始文本，方便人工核对

### 技术亮点

- **多策略图像预处理**：5 种预处理策略（原图、增强、去噪、二值化、锐化）自动选择最优结果
- **智能字符纠错**：针对型号/编号字段的字母数字混淆进行智能修正
- **模糊匹配**：内置常见设备名称和生产厂家库，支持模糊匹配
- **颜色标记**：不同类别设备用不同背景色区分（绿色-外检、蓝色-带回、红色-退检）

## 技术栈

### 后端技术

| 技术/库 | 版本 | 用途 |
|---------|------|------|
| Python | 3.x | 开发语言 |
| Flask | - | Web 框架 |
| Flask-CORS | - | 跨域支持 |
| EasyOCR | - | OCR 文字识别引擎（基于 PyTorch） |
| PyTorch | - | 深度学习框架 |
| OpenCV (cv2) | - | 图像处理 |
| NumPy | - | 数值计算 |
| OpenPyXL | - | Excel 文件生成 |

### 前端技术

| 技术 | 用途 |
|------|------|
| HTML5 | 页面结构 |
| Tailwind CSS (CDN) | 样式框架 |
| JavaScript (原生) | 交互逻辑 |

### OCR 引擎说明

系统使用 **EasyOCR** 作为 OCR 识别引擎，原因如下：

- 支持中英文混合识别
- 基于 PyTorch，兼容性好
- CPU 即可运行，无需 GPU
- 识别准确率高

> **注意**：系统最初使用 PaddleOCR，但因 PaddlePaddle 3.x 与 oneDNN 的兼容性问题（`ConvertPirAttribute2RuntimeAttribute` 错误），切换为 EasyOCR 方案。

## 环境要求

### 硬件要求

- **CPU**：双核及以上（推荐四核以上以获得更好的识别速度）
- **内存**：4GB 及以上（OCR 模型加载约需 1-2GB）
- **磁盘空间**：至少 2GB 可用空间（模型文件约 100MB + 依赖库）

### 软件要求

- **操作系统**：Windows 10/11、Linux、macOS
- **Python**：3.8 ~ 3.12
- **浏览器**：Chrome、Edge、Firefox 等现代浏览器

### Python 依赖库

```
flask
flask-cors
easyocr
torch
torchvision
opencv-python
numpy
openpyxl
```

## 安装部署

### 第一步：安装 Python

1. 访问 [Python 官网](https://www.python.org/downloads/) 下载 Python 3.x
2. 运行安装程序，**勾选 "Add Python to PATH"**
3. 安装完成后，打开命令行验证：
   ```bash
   python --version
   ```

### 第二步：安装依赖库

在项目目录下打开命令行，执行：

```bash
pip install flask flask-cors easyocr opencv-python numpy openpyxl
```

> **注意**：
> - 首次安装 easyocr 时会自动安装 PyTorch，可能需要较长时间
> - 如果网络较慢，可以使用国内镜像源：
>   ```bash
>   pip install -i https://pypi.tuna.tsinghua.edu.cn/simple flask flask-cors easyocr opencv-python numpy openpyxl
>   ```

### 第三步：启动系统

1. 进入项目目录：
   ```bash
   cd 下场单
   ```

2. 运行应用：
   ```bash
   python app.py
   ```

3. 等待启动完成，看到类似以下输出表示启动成功：
   ```
   EasyOCR initialized successfully
    * Running on http://127.0.0.1:5000
   ```

4. 打开浏览器访问：http://127.0.0.1:5000

> **首次启动说明**：
> - 首次运行时，EasyOCR 会自动下载模型文件（约 100MB），请确保网络连接正常
> - 模型下载路径：`C:\Users\用户名\.EasyOCR\model\`
> - 下载完成后，后续启动会快很多

### 环境变量说明

系统会自动设置以下环境变量：

| 变量 | 值 | 说明 |
|------|----|------|
| KMP_DUPLICATE_LIB_OK | TRUE | 解决 OpenMP 运行库重复初始化的问题 |

## 使用指南

### 快速开始

1. **启动系统**：运行 `python app.py`，浏览器访问 http://127.0.0.1:5000
2. **填写公司信息**：在页面顶部表格中填写单位名称、联系人、地址等信息
3. **上传设备图片**：将设备铭牌图片拖拽到对应区域（外检/带回/退检）
4. **核对识别结果**：在设备明细表中核对和编辑识别出的设备信息
5. **导出 Excel**：点击"导出 Excel 表格"按钮，生成下场单文件

### 详细操作说明

#### 1. 填写公司信息

页面顶部的表格用于填写公司和下场单基本信息：

| 字段 | 说明 | 示例 |
|------|------|------|
| 公司-下场单 | 标题（可在代码中修改默认值） | XX公司-下场单 |
| 业务员 | 业务负责人姓名 | 张三 |
| 下场负责人 | 现场工作负责人 | 李四 |
| 单位名称 | 客户单位全称（必填） | XX科技有限公司 |
| 联系人 | 客户对接人 | 王经理 |
| 联系方式 | 客户电话 | 13800138000 |
| 单位地址 | 客户地址 | 北京市朝阳区XX路XX号 |
| 下场日期 | 计划下场日期 | 2024-01-15 |
| 下场人员 | 所有参加人员 | 张三、李四、王五 |
| 委托单备注 | 其他说明信息 | 请尽快完成检定 |
| 设备总数 | 设备总数量（自动统计） | 10 |
| 带回数量 | 带回设备数量（自动统计） | 3 |

#### 2. 上传设备图片

系统支持三种设备分类，每种对应不同的上传区域：

| 分类 | 颜色 | 说明 |
|------|------|------|
| 外检设备 | 绿色 | 现场检定，不需要带回 |
| 带回设备 | 蓝色 | 需要带回实验室检定 |
| 退检设备 | 红色 | 退检不做的设备 |

**上传方式**：
- **拖拽上传**：将图片文件直接拖拽到对应区域
- **点击上传**：点击区域内的"上传图片"按钮，选择文件
- **批量上传**：支持一次选择多张图片

**支持的图片格式**：PNG、JPG、JPEG、GIF、BMP、WebP、TIFF、TIF

**文件大小限制**：单张图片最大 10MB

#### 3. 查看识别状态

上传区域会显示：
- 已上传图片缩略图
- 上传数量 / 已识别数量
- 识别进度条

**状态说明**：
- 识别中：显示进度条和加载动画
- 识别成功：自动添加到设备明细表
- 识别失败：显示失败提示，可点击重试

#### 4. 编辑设备信息

识别完成后，设备信息会自动填入下方的设备明细表。你可以：

- **直接编辑**：点击任意单元格即可修改内容
- **添加行**：点击"+ 添加行"按钮手动添加设备
- **删除行**：点击行末的"删除"按钮移除设备
- **自动编号**：序号会自动重新编排

**表格字段说明**：

| 字段 | 说明 |
|------|------|
| 序号 | 自动编号 |
| 证书日期 | 检定证书日期 |
| 设备名称 | 识别出的设备名称 |
| 规格型号 | 设备型号规格 |
| 出厂编号 | 设备出厂序列号 |
| 设备编号 | 内部资产编号 |
| 生产厂家 | 设备制造商 |
| 设备备注 | 特殊要求、校准信息、地点、温湿度等 |
| 负责人 | 该设备负责人（**无背景色**） |
| 操作 | 删除按钮 |

> **注意**："负责人"列不应用任何背景色标记，保持与普通列一致的视觉样式。

#### 5. 查看 OCR 原始内容

页面底部的"OCR识别原始内容"区域会显示每张图片识别出的原始文本，方便：
- 核对识别准确性
- 手动复制补充信息
- 排查识别问题

#### 6. 导出 Excel

1. 确认所有信息填写正确
2. 点击"导出 Excel 表格"按钮
3. 浏览器会自动下载生成的 Excel 文件
4. 文件命名格式：`日期_单位名称.xlsx`（例如：`2024-01-15_XX科技有限公司.xlsx`）

**Excel 表格格式说明**：
- 完全按照模板格式生成
- 不同类别的设备用不同背景色标记
- 字体使用"华文仿宋"
- 列宽和行高与模板一致

### 操作技巧

1. **提高识别准确率**：
   - 确保图片清晰，铭牌文字完整
   - 光线充足，避免反光和阴影
   - 拍摄角度尽量正对铭牌
   - 图片中文字占比适中，不要过大或过小

2. **批量处理**：
   - 可以一次上传多张图片，系统会依次识别
   - 识别过程中可以继续填写其他信息

3. **快速修改**：
   - 识别结果仅供参考，请务必人工核对
   - 直接在表格中编辑比重新上传更高效

## API 接口文档

### 1. 首页

**接口地址**：`GET /`

**说明**：返回系统主页

---

### 2. 图片上传识别

**接口地址**：`POST /upload`

**请求方式**：multipart/form-data

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| file | File | 是 | 图片文件 |
| category | String | 否 | 设备类别：`inspected`(外检，默认)、`brought_back`(带回)、`returned`(退检) |

**响应示例**：

```json
{
  "device_name": "压力表",
  "model": "Y-100",
  "factory_number": "202401001",
  "device_number": "",
  "manufacturer": "上海自动化仪表厂",
  "confidence": {
    "device_name": 0.9,
    "model": 0.8,
    "factory_number": 0.95,
    "manufacturer": 0.95
  },
  "category": "inspected",
  "ocr_text": "设备名称：压力表\n型号规格：Y-100\n出厂编号：202401001\n...",
  "filename": "xxx_test.jpg",
  "ocr_engine": "easyocr",
  "success": true,
  "timing": {
    "decode_time": 0.002,
    "ocr_time": 2.345,
    "extract_time": 0.003,
    "total_time": 2.35
  }
}
```

**错误响应**：

```json
{
  "error": "错误描述",
  "error_code": "错误代码"
}
```

**错误代码说明**：

| 错误代码 | 说明 |
|----------|------|
| no_file | 未上传文件 |
| empty_filename | 文件名为空 |
| invalid_file_type | 不支持的文件格式 |
| empty_file | 文件内容为空 |
| file_too_large | 文件大小超过限制 |
| ocr_init_failed | OCR 引擎初始化失败 |
| image_decode_failed | 图片解码失败 |
| no_text_detected | 未检测到文字 |
| server_error | 服务器内部错误 |

---

### 3. 生成 Excel

**接口地址**：`POST /generate_excel`

**请求方式**：application/json

**请求参数**：

```json
{
  "company_info": {
    "company": "公司-下场单",
    "salesperson": "张三",
    "field_leader": "李四",
    "company_name": "XX科技有限公司",
    "contact_person": "王经理",
    "contact_phone": "13800138000",
    "address": "北京市朝阳区XX路XX号",
    "field_date": "2024-01-15",
    "field_personnel": "张三、李四、王五",
    "remark": "请尽快完成检定"
  },
  "devices": [
    {
      "cert_date": "2024-01-15",
      "device_name": "压力表",
      "model": "Y-100",
      "factory_number": "202401001",
      "device_number": "EQ-001",
      "manufacturer": "上海自动化仪表厂",
      "remark": "精度1.6级，0-1.6MPa",
      "responsible_person": "张三",
      "category": "inspected"
    }
  ]
}
```

**响应示例**：

```json
{
  "filename": "2024-01-15_XX科技有限公司.xlsx",
  "generate_time": 0.047,
  "success": true
}
```

---

### 4. 下载 Excel

**接口地址**：`GET /download/<filename>`

**说明**：下载生成的 Excel 文件

## 常见问题

### Q1: 首次启动很慢，卡在模型下载怎么办？

**A**: 首次运行 EasyOCR 需要下载模型文件（约 100MB），请耐心等待。如果网络不好，可以：
- 使用手机热点或其他网络
- 手动下载模型文件放到 `C:\Users\用户名\.EasyOCR\model\` 目录下

### Q2: 识别准确率不高怎么办？

**A**: 可以尝试以下方法提高准确率：
1. 确保图片清晰，对焦准确
2. 光线均匀，避免反光和阴影
3. 拍摄角度正对铭牌，不要倾斜太多
4. 图片中文字大小适中，不要太小
5. 确保铭牌干净，没有污渍和遮挡

### Q3: 中文识别效果不好怎么办？

**A**: EasyOCR 支持简体中文识别，如果效果不好：
1. 检查是否下载了中文模型（`zh_sim_g2.pth`）
2. 确保图片中的中文清晰可辨
3. 可以手动编辑识别结果

### Q4: 启动报错 "OMP: Error #15: Initializing libiomp5md.dll"

**A**: 这是 OpenMP 库冲突问题，系统已自动设置 `KMP_DUPLICATE_LIB_OK=TRUE` 环境变量来解决。如果仍有问题，可以尝试：
- 重新安装 PyTorch
- 检查是否有多个 Python 环境冲突

### Q5: 上传图片后识别失败怎么办？

**A**: 识别失败可能的原因：
1. 图片格式不支持（支持 PNG、JPG、JPEG 等）
2. 图片文件损坏
3. 图片中没有可识别的文字
4. OCR 引擎异常

**解决方法**：
- 检查图片格式和完整性
- 尝试用其他图片测试
- 查看控制台输出的错误信息
- 点击"重试失败"按钮重新识别

### Q6: Excel 导出后打不开怎么办？

**A**: 请检查：
1. Excel 文件是否完整下载
2. 是否安装了 Microsoft Excel 或 WPS
3. 文件名中是否有特殊字符（系统会自动处理非法字符）

### Q7: 如何修改 Excel 模板格式？

**A**: Excel 格式在 [app.py](file:///c:/Users/he/Desktop/下场单/app.py) 中定义，可以修改：
- `COLUMN_WIDTHS`：列宽设置
- `ROW_HEIGHTS`：行高设置
- `header_font` / `title_font`：字体设置
- `color_fills`：颜色设置
- 表格生成逻辑：`generate_excel()` 函数

### Q8: 可以在局域网内其他电脑访问吗？

**A**: 可以。系统监听 `0.0.0.0:5000`，局域网内其他电脑可以通过你的 IP 地址访问，例如：
`http://192.168.1.100:5000`

> 注意：请确保防火墙允许 5000 端口访问。

### Q9: 如何添加更多常见设备名称？

**A**: 在 [app.py](file:///c:/Users/he/Desktop/下场单/app.py) 的 `COMMON_DEVICE_NAMES` 列表中添加即可。

### Q10: 系统支持 GPU 加速吗？

**A**: 目前默认使用 CPU 运行。如果有 NVIDIA GPU，可以：
1. 安装 CUDA 版本的 PyTorch
2. 修改 `init_ocr()` 函数中的 `gpu=False` 为 `gpu=True`

## 目录结构

```
下场单/
├── app.py                    # 主应用程序（Flask 后端 + OCR 逻辑）
├── templates/
│   ├── index.html            # 电脑端页面
│   └── mobile/
│       └── index.html        # 移动端页面
├── uploads/                  # 上传的图片（自动创建）
├── output/                   # 生成的 Excel 文件（自动创建）
├── __pycache__/              # Python 缓存（自动创建）
├── 下场单模版（完整版）.xlsx  # 参考模板
├── 使用教程.md               # 本文档
├── 部署指南.md               # 部署指南
├── Dockerfile                # Docker 部署配置
├── .dockerignore             # Docker 忽略文件
├── requirements.txt          # Python 依赖列表
├── api.csv.example           # API 配置示例文件
├── .gitignore                # Git 忽略文件
└── README.md                 # 项目说明
```

## 核心函数说明

### OCR 相关

| 函数 | 位置 | 说明 |
|------|------|------|
| `init_ocr()` | app.py L29 | 初始化 OCR 引擎 |
| `ocr_image(image_bytes)` | app.py L221 | 图片 OCR 识别主函数 |
| `run_ocr_easyocr(img)` | app.py L207 | EasyOCR 识别调用 |
| `extract_device_info(text)` | app.py L295 | 从 OCR 文本中提取设备信息 |

### 图像预处理

| 函数 | 位置 | 说明 |
|------|------|------|
| `preprocess_enhanced(img)` | app.py L168 | CLAHE 对比度增强 |
| `preprocess_denoise(img)` | app.py L178 | 非局部均值去噪 |
| `preprocess_threshold(img)` | app.py L187 | Otsu 二值化 |
| `preprocess_sharpen(img)` | app.py L197 | 图像锐化 |
| `calculate_text_quality(text)` | app.py L148 | 计算 OCR 文本质量评分 |

### 纠错处理

| 函数 | 位置 | 说明 |
|------|------|------|
| `correct_ocr_text(text)` | app.py L130 | 标点符号纠错 |
| `correct_alnum(text)` | app.py L137 | 字母数字混淆纠错 |

### API 接口

| 函数 | 位置 | 说明 |
|------|------|------|
| `index()` | app.py L440 | 首页路由 |
| `upload_image()` | app.py L444 | 图片上传识别接口 |
| `generate_excel()` | app.py L507 | 生成 Excel 接口 |
| `download_file()` | app.py L671 | 下载 Excel 接口 |

## 更新日志

### v1.0.0 (2024-07-15)

- 初始版本发布
- 支持图片 OCR 识别和设备信息提取
- 支持三种设备分类（外检/带回/退检）
- 支持 Excel 一键导出
- OCR 引擎从 PaddleOCR 切换为 EasyOCR，解决兼容性问题
- 多策略图像预处理，提高识别准确率
- 智能字符纠错和模糊匹配

---

**技术支持**：如遇问题，请查看控制台输出的错误信息，或参考本文档的"常见问题"章节。
